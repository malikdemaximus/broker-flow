export const cities = (state) => state.cities

export const servicePoints = (state) => state.servicePoints

export const paymentTypes = (state) => state.paymentTypes

export const productCategories = (state) => state.productCategories

export default {
  cities,
  servicePoints,
  paymentTypes,
  productCategories,
}
